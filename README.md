# Suraj Sharma — Portfolio (Stripe-like theme)

This is a Next.js App Router portfolio starter generated automatically.

## Features
- Next.js 14 App Router
- TailwindCSS (configured)
- Framer Motion animations
- Dark/Light mode toggle (floating button)
- Hero, About, Skills, Experience, Projects, Testimonials, Contact, Footer
- Project case study pages
- Replace images in /public

## Run locally
1. npm install
2. npm run dev
3. Open http://localhost:3000

Replace the placeholder images in `public/` with real images and update content as needed.
